function [BoxWidth, BoxHeight, BoxMass] = defaultBoxParameters(obj)
%DEFAULTBOXPARAMETERS returns the default box parameters for the lifting
%task.
%
%The numerical values are consistent with the experiment design.
%
%   [BoxWidth, BoxHeight, BoxMass] = DEFAULTBOXPARAMETERS(obj)
%   

% Default values
BoxWidth = 0.3300;
BoxHeight = 0.2480;
BoxMass = 10.5000;

end

